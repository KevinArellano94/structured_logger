/**
 * Main entry point for the logger library.
 * Exports the Logger class and types for easy import.
 */
export { Logger } from "./src/logger.ts";
export type { LogEntry, LoggerOptions, LogLevel } from "./src/types.ts";
